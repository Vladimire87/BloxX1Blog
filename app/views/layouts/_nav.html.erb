      <nav class="py-2 bg-body-tertiary border-bottom">
  <div class="container d-flex flex-wrap">
    <div class="navbar w-100">
      <ul class="nav me-auto">
        <a class="navbar-brand" href="<%=root_path%>">
          <%= image_tag 'BloxX1-logos_black.png' , height: "25" , class: "d-inline-block align-text-top" %>
          <%# <img src="" width="30" height="24" class="d-inline-block align-text-top">
                           BloxX1 %>
        </a>
        <li class="nav-item"><a href="<%=root_path%>" class="nav-link link-body-emphasis px-2 active"
                        aria-current="page">Home</a></li>
        <li class="nav-item"><a href="<%= new_article_path %>" class="nav-link link-body-emphasis px-2">New
            Article</a></li>
      </ul>
      <div class="dropdown d-flex justify-content-end">
        <% if user_signed_in? %>
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                        aria-expanded="false">
            <%=current_user.username%>
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="<%=destroy_user_session_path%>"
                              data-turbo-method="delete">Sign out</a></li>
          <% else %>
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                              aria-expanded="false">
              Hello!
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="<%=new_user_session_path%>">Login</a></li>
              <li><a class="dropdown-item" href="<%=new_user_registration_path%>">Sign up</a></li>
            </ul>
          <% end %>
        </div>
      </div>
    </nav>
